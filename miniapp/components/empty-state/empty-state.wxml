<view class="empty-state">
  <image class="empty-image" src="{{image}}" mode="aspectFit" />
  <text class="empty-text">{{text}}</text>
  <button wx:if="{{showButton}}" class="empty-button" bindtap="onButtonTap">{{buttonText}}</button>
</view>
